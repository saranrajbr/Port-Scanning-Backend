def arp_scan(target):

    terminal = []
    terminal.append("ARP scan not supported For now .....")

    table = []

    stats = {
        "active_hosts": 0,
        "threats": 0
    }

    return table, stats, terminal